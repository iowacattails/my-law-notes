name: Publish Law Note
on:
  repository_dispatch:
    types: [publish-law-note]

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Publish
        env:
          GITHUB_TOKEN: ${{ secrets.TOKEN }}
        run: |
          python3 -c "
          import os, json, base64, requests
          data = json.loads(os.environ['GITHUB_EVENT_PATH'])['client_payload']
          # ... your exact v4.1 publishing logic here ...
          "
